%h1 Listing contractors

%table
  %thead
    %tr
      %th First name
      %th Last name
      %th Shop name
      %th Is active
      %th
      %th
      %th

  %tbody
    - @contractors.each do |contractor|
      %tr
        %td= contractor.first_name
        %td= contractor.last_name
        %td= contractor.shop_name
        %td= contractor.is_active
        %td= link_to 'Show', contractor
        %td= link_to 'Edit', edit_contractor_path(contractor)
        %td= link_to 'Destroy', contractor, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Contractor', new_contractor_path
